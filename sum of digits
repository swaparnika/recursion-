#include<stdio.h>
int main()
{
  int n;
  printf("Enter the number n-");
  scanf("%d",&n):
  printf("The sum of digits in %d is %d ",n,sum(n));
}
int sum(int n)
{
 if(n==0)
  return 0:
 else 
  return (n%10+sum(n/10));
 }
